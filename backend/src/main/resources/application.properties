

server.port=${PORT:8080}

spring.datasource.url=jdbc:mysql://localhost:3306/sucedb
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
app.cloudinary.cloud-name=yrru3ddk
app.cloudinary.api-key=626752427555362
app.cloudinary.api-secret=RMnNkMxPNj7oG4RcvhdkFlX4erM
app.jwt.secret=SuceSecretKey2024SuceSecretKey2024SuceSecretKey2024
app.jwt.access-expiry-ms=900000
app.jwt.refresh-expiry-ms=604800000

app.cors.origins=http://localhost:5173,http://localhost:3000

# Used to build links in emails (password reset, etc.) — set to wherever
# your frontend actually runs.
app.frontend-url=http://localhost:5173

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
app.upload.dir=uploads/products

# ── Mail (Gmail SMTP) — used by OtpService ─────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kathichandra441@gmail.com
spring.mail.password=xdkpfsffdsjanqup
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ── SMS mock — keep true until a real SMS provider is wired up ────

app.sms.mock=true
